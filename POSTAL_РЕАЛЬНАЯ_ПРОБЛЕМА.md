# 🔍 РЕАЛЬНАЯ ПРОБЛЕМА: Почему Postal не работает

## ✅ ОТКУДА БРАЛИСЬ ПЕРЕМЕННЫЕ

**Переменные работали через ENV в docker-compose.yml!**

В `docker-compose.yml` Postal настроен с переменными окружения:
```yaml
postal:
  environment:
    POSTAL_MAIN_DB_PASSWORD: ${MARIADB_PASSWORD}  # ← Подставляется из .env
    POSTAL_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD}  # ← Подставляется из .env
    POSTAL_RAILS_SECRET_KEY: ${SECRET_KEY_BASE}  # ← Подставляется из .env
```

**Это работает!** Docker Compose автоматически подставляет значения из `.env` файла.

---

## 🔴 ПРОБЛЕМА: Конфликт между ENV и файлом

**НО также монтируется файл `postal.yml`:**
```yaml
volumes:
  - ./config/postal.yml:/opt/postal/config/postal.yml:ro
```

**Что происходит:**
1. Postal поддерживает ДВА способа конфигурации:
   - ✅ Через переменные окружения `POSTAL_*` (работает!)
   - ❌ Через файл `postal.yml` (если файл существует, может переопределить ENV)

2. Если файл `postal.yml` существует и содержит `${MARIADB_PASSWORD}`, Postal может:
   - Читать файл вместо ENV переменных
   - Использовать буквально `${MARIADB_PASSWORD}` как пароль
   - Игнорировать правильные ENV переменные

3. **Результат:** Postal не может подключиться к БД, потому что использует неправильный пароль из файла

---

## ✅ РЕШЕНИЕ: Убрать файл postal.yml

**Postal должен использовать ТОЛЬКО ENV переменные!**

### Вариант 1: Убрать монтирование postal.yml (РЕКОМЕНДУЕТСЯ)

**Изменить `docker-compose.yml`:**

```yaml
postal:
  # ... остальная конфигурация ...
  environment:
    POSTAL_MAIN_DB_PASSWORD: ${MARIADB_PASSWORD}
    POSTAL_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD}
    POSTAL_RAILS_SECRET_KEY: ${SECRET_KEY_BASE}
    # ... остальные POSTAL_* переменные ...
  volumes:
    # УБРАТЬ эти строки:
    # - ./config/postal.yml:/config/postal.yml:ro
    # - ./config/postal.yml:/opt/postal/config/postal.yml:ro
    
    # Оставить только:
    - postal_data:/opt/postal/data
```

**После этого:**
- Postal будет использовать только ENV переменные
- Все значения будут браться из `.env` через docker-compose
- Файл `postal.yml` больше не нужен

---

### Вариант 2: Правильно сгенерировать postal.yml

**Если нужно оставить файл (не рекомендуется):**

```bash
# Использовать скрипт для генерации
./scripts/generate-postal-config.sh

# Или вручную:
source .env
envsubst < config/postal.yml.example > config/postal.yml
```

**НО:** Это создает дублирование - одни и те же настройки в двух местах (ENV и файл).

---

## 🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ

### Шаг 1: Проверить текущую конфигурацию

```bash
# Проверить что ENV переменные есть в docker-compose
grep "POSTAL_MAIN_DB_PASSWORD" docker-compose.yml
# Должно быть: POSTAL_MAIN_DB_PASSWORD: ${MARIADB_PASSWORD}

# Проверить что postal.yml монтируется
grep "postal.yml" docker-compose.yml
# Если находит - это проблема!
```

### Шаг 2: Убрать монтирование postal.yml

**Отредактировать `docker-compose.yml`:**

Найти секцию `postal:` → `volumes:` и удалить строки:
```yaml
- ./config/postal.yml:/config/postal.yml:ro
- ./config/postal.yml:/opt/postal/config/postal.yml:ro
```

### Шаг 3: Перезапустить Postal

```bash
docker compose restart postal

# Подождать 10 секунд
sleep 10

# Проверить логи
docker compose logs postal --tail=50 | grep -i "error\|denied\|mysql"
```

---

## 🧪 ПРОВЕРКА

### Проверка 1: ENV переменные в контейнере

```bash
# Проверить что переменные есть в контейнере
docker compose exec postal env | grep POSTAL_MAIN_DB_PASSWORD

# Должно показать реальное значение, НЕ ${MARIADB_PASSWORD}
```

### Проверка 2: Файл postal.yml не используется

```bash
# Проверить что файл не монтируется
docker compose exec postal ls -la /opt/postal/config/postal.yml

# Если файл не существует - хорошо!
# Если существует - нужно убрать из docker-compose.yml
```

### Проверка 3: Postal подключается к БД

```bash
# Проверить логи на ошибки подключения
docker compose logs postal --tail=50 | grep -i "mysql\|database\|denied"

# Не должно быть:
# - "Access denied for user 'postal'@'%'"
# - "Can't connect to MySQL server"
```

---

## 📊 ПРИОРИТЕТ

### 🔴 Критично:
1. **Убрать монтирование postal.yml из docker-compose.yml**
   - Это основная причина проблемы
   - Postal должен использовать только ENV переменные

### 🟡 Важно:
2. Проверить что все `POSTAL_*` переменные есть в docker-compose.yml
3. Перезапустить Postal после изменений

### 🟢 Опционально:
4. Удалить файл `config/postal.yml` (больше не нужен)
5. Можно оставить `postal.yml.example` как шаблон для документации

---

## ✅ ИТОГО

**Проблема НЕ в том, что переменные не подставляются!**

**Проблема в том, что:**
- ✅ ENV переменные работают правильно
- ❌ Но файл `postal.yml` переопределяет их
- ❌ И в файле переменные не подставлены

**Решение:**
- Убрать монтирование `postal.yml` из docker-compose.yml
- Использовать только ENV переменные (как и было задумано)

---

**Готово!** После этого Postal должен работать через ENV переменные. 🎉
