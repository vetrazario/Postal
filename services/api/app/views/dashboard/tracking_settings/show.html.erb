<div class="space-y-10">
  <div>
    <h1 class="text-3xl font-bold text-gray-900">Tracking Settings</h1>
    <p class="mt-2 text-sm text-gray-700">Configure email tracking and monitoring</p>
  </div>

  <% if flash[:notice] %>
    <div class="rounded-md bg-green-50 p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <p class="text-sm font-medium text-green-800"><%= flash[:notice] %></p>
        </div>
      </div>
    </div>
  <% end %>

  <% if flash[:error] %>
    <div class="rounded-md bg-red-50 p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <p class="text-sm font-medium text-red-800"><%= flash[:error] %></p>
        </div>
      </div>
    </div>
  <% end %>

  <!-- Tracking Statistics -->
  <div class="rounded-lg bg-white shadow">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-lg font-medium leading-6 text-gray-900 mb-6">Current Statistics</h3>

      <div class="grid grid-cols-1 gap-5 sm:grid-cols-3">
        <!-- Total Clicks -->
        <div class="rounded-lg bg-blue-50 p-5">
          <div class="flex items-center justify-between">
            <div>
              <dt class="text-sm font-medium text-blue-700">Total Clicks</dt>
              <dd class="mt-2 text-3xl font-semibold text-blue-900"><%= EmailClick.clicked.count rescue 0 %></dd>
            </div>
            <svg class="h-8 w-8 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 15l-2 5L9 9l11 4-5 2zm0 0l5 5M7.188 2.239l.777 2.897M5.136 7.965l-2.898-.777M13.95 4.05l-2.122 2.122m-5.657 5.656l-2.12 2.122" />
            </svg>
          </div>
        </div>

        <!-- Total Opens -->
        <div class="rounded-lg bg-green-50 p-5">
          <div class="flex items-center justify-between">
            <div>
              <dt class="text-sm font-medium text-green-700">Total Opens</dt>
              <dd class="mt-2 text-3xl font-semibold text-green-900"><%= EmailOpen.opened.count rescue 0 %></dd>
            </div>
            <svg class="h-8 w-8 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 19v-8.93a2 2 0 01.89-1.664l7-4.666a2 2 0 012.22 0l7 4.666A2 2 0 0121 10.07V19M3 19a2 2 0 002 2h14a2 2 0 002-2M3 19l6.75-4.5M21 19l-6.75-4.5M3 10l6.75 4.5M21 10l-6.75 4.5m0 0l-1.14.76a2 2 0 01-2.22 0l-1.14-.76" />
            </svg>
          </div>
        </div>

        <!-- Unique Opens -->
        <div class="rounded-lg bg-purple-50 p-5">
          <div class="flex items-center justify-between">
            <div>
              <dt class="text-sm font-medium text-purple-700">Unique Opens</dt>
              <dd class="mt-2 text-3xl font-semibold text-purple-900"><%= EmailOpen.unique_opens.count rescue 0 %></dd>
            </div>
            <svg class="h-8 w-8 text-purple-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Tracking Settings Form -->
  <div class="rounded-lg bg-white shadow">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-lg font-medium leading-6 text-gray-900 mb-6">Tracking Configuration</h3>

      <%= form_with url: dashboard_tracking_settings_path, method: :patch, local: true, class: "space-y-6" do |form| %>
        <!-- Open Tracking -->
        <div class="flex items-start">
          <div class="flex h-5 items-center">
            <%= check_box_tag 'tracking_settings[enable_open_tracking]', 'true', @tracking_settings[:enable_open_tracking], class: "h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-500" %>
          </div>
          <div class="ml-3 text-sm">
            <label for="tracking_settings_enable_open_tracking" class="font-medium text-gray-700">Enable Open Tracking</label>
            <p class="text-gray-500">Track when recipients open emails using invisible tracking pixels</p>
          </div>
        </div>

        <!-- Click Tracking -->
        <div class="flex items-start">
          <div class="flex h-5 items-center">
            <%= check_box_tag 'tracking_settings[enable_click_tracking]', 'true', @tracking_settings[:enable_click_tracking], class: "h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-500" %>
          </div>
          <div class="ml-3 text-sm">
            <label for="tracking_settings_enable_click_tracking" class="font-medium text-gray-700">Enable Click Tracking</label>
            <p class="text-gray-500">Replace links with tracking URLs to monitor clicks (secure redirect-based tracking)</p>
          </div>
        </div>

        <!-- Save Button -->
        <div class="flex justify-end">
          <%= form.submit "Save Tracking Settings", class: "rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600" %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- How It Works -->
  <div class="rounded-lg bg-blue-50 border border-blue-200">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-base font-semibold text-blue-900 mb-4">How Tracking Works</h3>

      <div class="space-y-4 text-sm text-blue-800">
        <div class="flex items-start">
          <svg class="h-5 w-5 text-blue-500 mt-0.5 mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <div>
            <strong>Click Tracking:</strong> All links are automatically replaced with secure tracking URLs (format: /go/readable-name-TOKEN). When clicked, the system records the click and redirects to the original URL.
          </div>
        </div>

        <div class="flex items-start">
          <svg class="h-5 w-5 text-blue-500 mt-0.5 mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <div>
            <strong>Open Tracking:</strong> A tiny invisible pixel is added to emails. When the email is opened, the pixel is loaded and we record the open event.
          </div>
        </div>

        <div class="flex items-start">
          <svg class="h-5 w-5 text-blue-500 mt-0.5 mr-2 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
          </svg>
          <div>
            <strong>Privacy & Security:</strong> Bot clicks are filtered automatically. All tracking uses secure, privacy-respecting methods. No personal data is collected beyond IP addresses and user agents.
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
