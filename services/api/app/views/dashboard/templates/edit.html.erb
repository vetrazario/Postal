<div class="space-y-6">
  <div>
    <h1 class="text-3xl font-bold tracking-tight text-gray-900">Edit Template</h1>
    <p class="mt-2 text-sm text-gray-700">Update template settings and content</p>
  </div>

  <div class="rounded-lg bg-white shadow-sm ring-1 ring-gray-900/5">
    <div class="px-4 py-6 sm:p-8">
      <%= form_with model: @template, url: dashboard_template_path(@template), method: :patch, class: "space-y-6" do |f| %>
        <% if @template.errors.any? %>
          <div class="rounded-md bg-red-50 p-4">
            <div class="flex">
              <div class="ml-3">
                <h3 class="text-sm font-medium text-red-800">
                  <%= pluralize(@template.errors.count, "error") %> prohibited this template from being saved:
                </h3>
                <div class="mt-2 text-sm text-red-700">
                  <ul class="list-disc pl-5 space-y-1">
                    <% @template.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <div>
          <%= f.label :name, class: "block text-sm font-medium leading-6 text-gray-900" %>
          <%= f.text_field :name, class: "mt-2 block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6" %>
        </div>

        <div>
          <%= f.label :external_id, class: "block text-sm font-medium leading-6 text-gray-900" %>
          <%= f.text_field :external_id, class: "mt-2 block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6" %>
        </div>

        <div>
          <%= f.label :html_content, "HTML Content", class: "block text-sm font-medium leading-6 text-gray-900" %>
          <%= f.text_area :html_content, rows: 12, class: "mt-2 block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6 font-mono" %>
        </div>

        <div class="flex items-center">
          <%= f.check_box :active, class: "h-4 w-4 rounded border-gray-300 text-indigo-600 focus:ring-indigo-600" %>
          <%= f.label :active, "Active", class: "ml-2 block text-sm text-gray-900" %>
        </div>

        <div class="flex gap-3">
          <%= f.submit "Update Template", class: "rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500" %>
          <%= link_to "Cancel", dashboard_templates_path, class: "rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
