; HAProxy Configuration
; Haraka can detect connections proxied through HAProxy
; We're not using HAProxy, so this is minimal config

[main]
; Enable HAProxy protocol support (disabled)
enabled=false

; List of trusted HAProxy servers (none)
hosts=
