; SMTP Authentication Configuration
; This configures the auth/flat_file plugin

[core]
; Supported authentication methods
methods=PLAIN,LOGIN

; NOTE: Actual authentication is handled by our custom smtp_auth plugin
; which checks credentials against PostgreSQL database
; This file is just for enabling the auth mechanism in Haraka
