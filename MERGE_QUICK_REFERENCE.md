# 🚀 Быстрая шпаргалка по мержу

## 📋 ПОРЯДОК МЕРЖА (строго!)

```
1. origin/claude/security-fixes-F6I8U      ⭐ КРИТИЧНО
2. origin/claude/fix-tracking-unsubscribe-links-Fr6Lf
3. origin/claude/fix-ai-analysis-display-6zxju
4. dashboard_only                          ⚠️ ОСТОРОЖНО!
5. DB_Fix                                  ⭐ КРИТИЧНО
```

---

## 🎯 ПРИ КОНФЛИКТАХ - ЧТО БРАТЬ?

### ✅ БРАТЬ ИЗ ДОПОЛНИТЕЛЬНОЙ ВЕТКИ (фиксы):

- `services/api/app/controllers/**/*.rb` - контроллеры
- `services/smtp-relay/**/*.js` - SMTP плагины
- `services/api/app/models/*.rb` - модели
- `services/api/app/services/**/*.rb` - сервисы
- `services/api/app/jobs/**/*.rb` - background jobs
- `services/api/app/views/**/*.erb` - views (кроме навигации!)
- `config/nginx.conf` - nginx конфиг
- `docker-compose.yml` - docker конфиг
- `scripts/**/*.sh` и `scripts/**/*.ps1` - скрипты

### ⚠️ БРАТЬ ИЗ MAIN (основной конфиг):

- `config/postal.yml` - основной конфиг Postal
- `.env` - секреты (не трогать!)

### 🔍 ПРОВЕРИТЬ ВРУЧНУЮ:

- `services/api/app/views/layouts/_dashboard_nav.html.erb` - навигация (может удалять элементы!)
- `services/api/db/migrate/*.rb` - миграции (объединить аккуратно)
- `*.md` и `docs/**/*.md` - документация (объединить обе версии)

---

## ⚠️ ОСОБЫЕ СЛУЧАИ

### dashboard_only - ВНИМАНИЕ!
Эта ветка **УДАЛЯЕТ** из навигации:
- SMTP Credentials
- API Keys  
- Webhooks

**Проверьте, нужно ли это!** Если нет - не берите эти изменения.

### DB_Fix
Уже содержит изменения из `claude/setup-email-testing-YifKd` - не мержить дважды!

---

## 📝 БЫСТРЫЙ ЧЕКЛИСТ

Перед каждым мержем:
- [ ] На ветке `main`
- [ ] `main` обновлена
- [ ] Нет незакоммиченных изменений

После каждого мержа:
- [ ] Все конфликты разрешены
- [ ] Merge commit создан
- [ ] Проверил логи

---

**Полная инструкция:** `MERGE_ORDER_WINDOWS.md`

