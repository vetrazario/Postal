# 🔍 КАК ПРОВЕРИТЬ СЕРВЕР НА ОШИБКИ

## Быстрый старт

У вас есть 3 способа проверить сервер:

### 1️⃣ БЫСТРАЯ АВТОМАТИЧЕСКАЯ ПРОВЕРКА (2-3 минуты)

```bash
./quick_check.sh
```

**Что проверяет:**
- ✅ База данных (подключение, таблицы, миграции)
- ✅ Docker socket exposure (критическая уязвимость)
- ✅ Webhook verification (bypass безопасности)
- ✅ Шифрование (слабые ключи)
- ✅ Memory limits (риск OOM)
- ✅ SMTP authentication
- ✅ Deprecated код

**Результат:**
- Цветной вывод с приоритетами проблем
- Exit code 0 = все ОК
- Exit code 1 = критические проблемы
- Exit code 2 = проблемы высокого приоритета

**Пример вывода:**
```
🔍 АВТОМАТИЧЕСКАЯ ПРОВЕРКА ПРОЕКТА POSTAL
==========================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣  БАЗА ДАННЫХ:
Проверка: Подключение к БД... ✅ OK
Проверка: Количество таблиц... ✅ OK
Проверка: Таблица api_keys... ✅ OK
...

📊 ИТОГОВАЯ СТАТИСТИКА
Проверено: 35 элементов
🔴 Критических проблем: 2
🟠 Высокий приоритет: 3
✅ Без проблем: 30
```

---

### 2️⃣ ДЕТАЛЬНАЯ ПРОВЕРКА С ОТЧЕТОМ (5-7 минут)

```bash
./detailed_check.sh
```

**Что проверяет:**
- 📊 Системная информация (Docker, Ruby, Rails версии)
- 🗄️ База данных (структура, миграции, данные)
- 🔒 Безопасность (все уязвимости)
- 🔧 ENV переменные (все требуемые)
- 💾 Память (limits и использование)
- 💻 Код (deprecated, anti-patterns)
- 📬 SMTP relay (конфигурация)
- 🌐 API и сервисы (health checks)
- ⚙️ Sidekiq (статистика)
- 📁 Файловая структура
- 📝 Логи (последние ошибки)

**Результат:**
- Создает файл `verification_results_YYYYMMDD_HHMMSS.txt`
- Содержит ~500+ строк детальной информации
- Дублирует вывод в консоль и файл

**Использование:**
```bash
# Запустить проверку
./detailed_check.sh

# Просмотреть результаты
cat verification_results_*.txt

# Или с пагинацией
less verification_results_*.txt

# Поиск проблем
grep "❌" verification_results_*.txt
```

---

### 3️⃣ РУЧНАЯ ПРОВЕРКА КОНКРЕТНЫХ ПРОБЛЕМ

Откройте файл `VERIFICATION_COMMANDS.md` - там есть команды для проверки каждой из 51 проблемы отдельно.

**Структура файла:**
- Критическая проблема #1-5
- Высокий приоритет #6-11
- Средний приоритет #12-21
- Комплексная проверка здоровья
- ENV переменные
- Security audit

**Пример проверки конкретной проблемы:**

```bash
# Проверить, смонтирован ли Docker socket
docker compose exec api ls -la /var/run/docker.sock

# Проверить webhook verification
docker compose exec api printenv SKIP_POSTAL_WEBHOOK_VERIFICATION

# Проверить список таблиц
docker compose exec api rails runner "puts ActiveRecord::Base.connection.tables.sort"
```

---

## 📋 ЧТО ДЕЛАТЬ ДАЛЬШЕ?

### Если `quick_check.sh` показал критические проблемы:

1. **Прочитайте файл `IMMEDIATE_FIXES.md`**
   - Там 5 критических исправлений
   - Время: ~30 минут
   - Пошаговые инструкции

2. **Выполните немедленные исправления:**
   ```bash
   # 1. Применить миграции
   docker compose exec api rails db:migrate

   # 2. Отредактировать docker-compose.yml
   # - Удалить строку с Docker socket
   # - Удалить SKIP_POSTAL_WEBHOOK_VERIFICATION

   # 3. Перезапустить
   docker compose down && docker compose up -d

   # 4. Проверить снова
   ./quick_check.sh
   ```

### Если есть проблемы высокого приоритета:

1. **Прочитайте `FULL_ERROR_ANALYSIS_REPORT.md`**
   - Секция "ВЫСОКИЙ ПРИОРИТЕТ"
   - Детальное описание каждой проблемы
   - Примеры кода и решения

2. **Запланируйте исправления**
   - N+1 queries
   - Weak encryption
   - Deprecated syntax
   - Memory limits

### Если все проверки прошли успешно:

**Поздравляем! 🎉**

Сервер в хорошем состоянии. Некоторые из "проблем" в отчете могут быть:
- Уже исправлены вами ранее
- Не применимы к вашей конфигурации
- Ложные срабатывания

**Рекомендуется:**
1. Все равно просмотрите отчет `detailed_check.sh`
2. Проверьте секции "Средний приоритет" и "Низкий приоритет"
3. Подумайте об улучшениях производительности и качества кода

---

## 🔄 РЕГУЛЯРНЫЕ ПРОВЕРКИ

Рекомендуется запускать проверки:

- **Ежедневно:** `quick_check.sh` (в составе CI/CD или cron)
- **Еженедельно:** `detailed_check.sh` (для мониторинга)
- **После каждого деплоя:** `quick_check.sh`
- **Перед major обновлениями:** `detailed_check.sh`

### Пример cron задачи:

```bash
# Запускать quick_check каждый день в 2 ночи
0 2 * * * cd /path/to/Postal && ./quick_check.sh >> /var/log/postal_check.log 2>&1

# Детальная проверка каждое воскресенье
0 3 * * 0 cd /path/to/Postal && ./detailed_check.sh
```

---

## 📊 ИНТЕРПРЕТАЦИЯ РЕЗУЛЬТАТОВ

### Коды выхода `quick_check.sh`:

- **0** = ✅ Все хорошо, критических проблем нет
- **1** = ❌ КРИТИЧНО - требуется немедленное действие
- **2** = ⚠️ WARNING - требуется внимание

### Маркеры в выводе:

- `✅` - Все в порядке
- `❌` - Критическая проблема
- `⚠️` - Предупреждение (высокий приоритет)
- `ℹ️` - Информация (средний приоритет)

### Приоритеты проблем:

1. **🔴 КРИТИЧЕСКИЕ** - исправить НЕМЕДЛЕННО (безопасность, data loss)
2. **🟠 ВЫСОКИЕ** - исправить в течение недели (производительность, стабильность)
3. **🟡 СРЕДНИЕ** - исправить в течение месяца (качество кода)
4. **🔵 НИЗКИЕ** - исправить когда удобно (code smells)

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Скрипты не запускаются:

```bash
# Сделать исполняемыми
chmod +x quick_check.sh detailed_check.sh

# Проверить права
ls -la *.sh
```

### Ошибка "docker compose command not found":

```bash
# Попробуйте с дефисом
docker-compose ps

# Или обновите Docker Desktop
```

### Ошибка "container not found":

```bash
# Проверить запущены ли контейнеры
docker compose ps

# Запустить если нужно
docker compose up -d

# Проверить имена контейнеров
docker ps --format "{{.Names}}"
```

### Скрипты работают слишком долго:

- `quick_check.sh` должен занимать 2-3 минуты
- `detailed_check.sh` должен занимать 5-7 минут
- Если дольше - возможно проблемы с производительностью БД

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

**Документация проекта:**
- `FULL_ERROR_ANALYSIS_REPORT.md` - полный отчет (51 проблема)
- `IMMEDIATE_FIXES.md` - критические исправления
- `VERIFICATION_COMMANDS.md` - все команды проверки
- `docs/TROUBLESHOOTING.md` - общие проблемы
- `docs/SECURITY.md` - политики безопасности

**Скрипты:**
- `quick_check.sh` - быстрая проверка
- `detailed_check.sh` - детальная проверка
- `install.sh` - установка проекта
- `run-migrations.sh` - применение миграций

---

## 💡 СОВЕТЫ

1. **Запускайте `quick_check.sh` перед изменениями**
   - Знайте исходное состояние
   - Проще найти что сломали

2. **Сохраняйте результаты `detailed_check.sh`**
   - Можно сравнить со временем
   - Видно тренды

3. **Автоматизируйте проверки**
   - Добавьте в CI/CD pipeline
   - Или в cron на сервере

4. **Не паникуйте**
   - Если сервер работает - значит не все так плохо
   - Проблемы в отчете - это рекомендации для улучшения
   - Приоритизируйте по критичности

5. **Проверяйте дважды**
   - Скрипты могут ошибаться
   - Критические проблемы проверьте вручную
   - Используйте `VERIFICATION_COMMANDS.md` для детальной проверки

---

## ✅ БЫСТРЫЙ ЧЕКЛИСТ

```bash
# 1. Быстрая проверка
./quick_check.sh

# 2. Если есть критические проблемы
cat IMMEDIATE_FIXES.md

# 3. Детальная проверка
./detailed_check.sh

# 4. Просмотр результатов
cat verification_results_*.txt | grep "❌"

# 5. Прочитать полный отчет
cat FULL_ERROR_ANALYSIS_REPORT.md

# 6. Исправить проблемы
# ... следовать инструкциям

# 7. Проверить снова
./quick_check.sh
```

---

**Вопросы?** Проверьте `VERIFICATION_COMMANDS.md` для детальных команд!

**Готовы к production?** Используйте чеклист в `FULL_ERROR_ANALYSIS_REPORT.md`!
