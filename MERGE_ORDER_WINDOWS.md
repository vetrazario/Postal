# Порядок мержа веток в Windows (GUI)

**Для использования в:**
- Visual Studio Code
- GitHub Desktop
- GitKraken
- SourceTree
- Другие Git GUI

---

## 📋 ПОРЯДОК МЕРЖА (строго по порядку!)

### Шаг 1: Подготовка
1. Откройте проект в вашей Git программе
2. Переключитесь на ветку `main`
3. Обновите `main`: `Pull` или `Fetch + Merge`

---

### Шаг 2: Мерж `origin/claude/security-fixes-F6I8U` ⭐ ВЫСШИЙ ПРИОРИТЕТ

**Почему первым:** Содержит критические фиксы безопасности и важные улучшения

**Действия:**
1. В Git GUI найдите ветку `origin/claude/security-fixes-F6I8U`
2. Выберите `Merge` или `Merge into current branch`
3. Если есть конфликты - см. таблицу приоритетов ниже

**Что содержит:**
- Фиксы безопасности
- Улучшения SMTP relay
- Обработка webhooks от Postal
- Улучшения AI Analytics
- Исправления в dashboard

---

### Шаг 3: Мерж `origin/claude/fix-tracking-unsubscribe-links-Fr6Lf`

**Действия:**
1. Убедитесь, что вы на ветке `main`
2. Мержите `origin/claude/fix-tracking-unsubscribe-links-Fr6Lf`
3. Разрешайте конфликты по таблице

**Что содержит:**
- Фикс трекинга (HTTPS)
- Функциональность unsubscribe

---

### Шаг 4: Мерж `origin/claude/fix-ai-analysis-display-6zxju`

**Действия:**
1. Мержите `origin/claude/fix-ai-analysis-display-6zxju`
2. Разрешайте конфликты по таблице

**Что содержит:**
- Исправление отображения AI анализа
- Правильный UI для результатов

---

### Шаг 5: Мерж `dashboard_only` ⚠️ ВНИМАНИЕ!

**Действия:**
1. Мержите `dashboard_only`
2. **ВАЖНО:** Эта ветка УДАЛЯЕТ некоторые элементы навигации
3. Проверьте, что это нужно!

**Что содержит:**
- Изменения в dashboard навигации
- Удаление SMTP Credentials, API Keys, Webhooks из сайдбара

---

### Шаг 6: Мерж `DB_Fix` ⭐ ВЫСОКИЙ ПРИОРИТЕТ

**Действия:**
1. Мержите `DB_Fix`
2. Разрешайте конфликты по таблице

**Что содержит:**
- Фиксы безопасности (удаление AMS заголовков)
- Фиксы базы данных
- Уже содержит изменения из `claude/setup-email-testing-YifKd`

---

## 🎯 ТАБЛИЦА ПРИОРИТЕТОВ ПРИ КОНФЛИКТАХ

Когда Git GUI покажет конфликт, используйте эту таблицу для решения:

| Файл/Папка | Приоритет | Что брать | Комментарий |
|------------|-----------|-----------|-------------|
| **Безопасность** |
| `services/api/app/controllers/**/*_controller.rb` | **Доп. ветка** | Из дополнительной ветки | Фиксы безопасности важнее |
| `services/smtp-relay/**/*.js` | **Доп. ветка** | Из дополнительной ветки | SMTP фиксы критичны |
| `config/**/*.yml` | **Доп. ветка** | Из дополнительной ветки | Конфигурационные фиксы |
| `docker-compose.yml` | **Доп. ветка** | Из дополнительной ветки | Docker фиксы |
| **Dashboard** |
| `services/api/app/views/dashboard/**/*.erb` | **Доп. ветка** | Из дополнительной ветки | UI улучшения |
| `services/api/app/views/layouts/_dashboard_nav.html.erb` | **Осторожно!** | Проверить вручную | Может удалять элементы |
| `services/api/app/controllers/dashboard/**/*.rb` | **Доп. ветка** | Из дополнительной ветки | Логика dashboard |
| **База данных** |
| `services/api/db/migrate/*.rb` | **Обе версии** | Вручную объединить | Миграции нужно аккуратно |
| `services/api/db/schema.rb` | **Доп. ветка** | Из дополнительной ветки | Схема обновляется автоматически |
| **Модели и сервисы** |
| `services/api/app/models/*.rb` | **Доп. ветка** | Из дополнительной ветки | Бизнес-логика |
| `services/api/app/services/**/*.rb` | **Доп. ветка** | Из дополнительной ветки | Сервисы |
| `services/api/app/jobs/**/*.rb` | **Доп. ветка** | Из дополнительной ветки | Background jobs |
| **Конфигурация** |
| `config/nginx.conf` | **Доп. ветка** | Из дополнительной ветки | Nginx фиксы |
| `config/postal.yml` | **Main** | Из main | Основной конфиг (если не фикс) |
| `.env` | **Main** | Из main | Секреты не трогать |
| **Документация** |
| `*.md` | **Обе версии** | Вручную объединить | Документация может дополняться |
| `docs/**/*.md` | **Обе версии** | Вручную объединить | Документация |
| **Скрипты** |
| `scripts/**/*.sh` | **Доп. ветка** | Из дополнительной ветки | Скрипты улучшений |
| `scripts/**/*.ps1` | **Доп. ветка** | Из дополнительной ветки | PowerShell скрипты |
| **Общее правило** |
| Если файл не в таблице | **Доп. ветка** | Из дополнительной ветки | Новые фиксы важнее |

---

## 🔧 ИНСТРУКЦИЯ ПО РАЗРЕШЕНИЮ КОНФЛИКТОВ В GUI

### В Visual Studio Code:

1. **Откройте файл с конфликтом**
   - Git GUI покажет файлы с конфликтами
   - Откройте файл в редакторе

2. **Вы увидите маркеры:**
   ```
   <<<<<<< HEAD (или main)
   Код из main
   =======
   Код из дополнительной ветки
   >>>>>>> branch-name
   ```

3. **Выберите версию:**
   - **Accept Current Change** - взять из main
   - **Accept Incoming Change** - взять из дополнительной ветки
   - **Accept Both Changes** - взять обе версии
   - **Compare Changes** - сравнить изменения

4. **Используйте таблицу приоритетов выше**

5. **Сохраните файл**

6. **Пометить как разрешенный:**
   - В Git GUI найдите файл
   - Выберите "Mark as resolved" или "Stage"

### В GitHub Desktop:

1. **Конфликты показываются в списке файлов**
2. **Откройте файл** - откроется в редакторе
3. **Используйте кнопки:**
   - "Use ours" (main)
   - "Use theirs" (дополнительная ветка)
   - Или редактируйте вручную
4. **Сохраните и закоммитьте**

---

## 📝 ПОШАГОВАЯ ИНСТРУКЦИЯ ДЛЯ КАЖДОГО МЕРЖА

### Мерж 1: `origin/claude/security-fixes-F6I8U`

**Ожидаемые конфликты:**
- Возможно в `services/api/app/controllers/`
- Возможно в `services/smtp-relay/plugins/`
- Возможно в `docker-compose.yml`

**Действия при конфликтах:**
1. Для контроллеров → **Брать из security-fixes** (доп. ветка)
2. Для SMTP плагинов → **Брать из security-fixes** (доп. ветка)
3. Для docker-compose → **Брать из security-fixes** (доп. ветка)
4. Для документации → **Объединить вручную**

**После мержа:**
- Проверьте, что все файлы разрешены
- Создайте merge commit
- Проверьте логи: `git log --oneline -5`

---

### Мерж 2: `origin/claude/fix-tracking-unsubscribe-links-Fr6Lf`

**Ожидаемые конфликты:**
- Возможно в `services/tracking/`
- Возможно в `services/api/app/controllers/api/v1/`

**Действия при конфликтах:**
1. Для tracking → **Брать из fix-tracking** (доп. ветка)
2. Для API контроллеров → **Брать из fix-tracking** (доп. ветка)

---

### Мерж 3: `origin/claude/fix-ai-analysis-display-6zxju`

**Ожидаемые конфликты:**
- Возможно в `services/api/app/controllers/dashboard/ai_analytics_controller.rb`
- Возможно в `services/api/app/views/dashboard/ai_analytics/`

**Действия при конфликтах:**
1. Для AI контроллера → **Брать из fix-ai-analysis** (доп. ветка)
2. Для AI views → **Брать из fix-ai-analysis** (доп. ветка)

---

### Мерж 4: `dashboard_only` ⚠️

**Ожидаемые конфликты:**
- Скорее всего в `services/api/app/views/layouts/_dashboard_nav.html.erb`
- Возможно в `services/api/app/views/dashboard/dashboard/index.html.erb`

**Действия при конфликтах:**
1. **ВНИМАНИЕ:** Эта ветка УДАЛЯЕТ элементы навигации
2. Проверьте, что удаление нужно
3. Если нужно удаление → **Брать из dashboard_only**
4. Если не нужно удаление → **Брать из main** или **объединить вручную**

**Проверьте:**
- Удаляются ли SMTP Credentials из навигации?
- Удаляются ли API Keys из навигации?
- Удаляются ли Webhooks из навигации?
- Если да - это правильно? Если нет - не берите эти изменения!

---

### Мерж 5: `DB_Fix`

**Ожидаемые конфликты:**
- Возможно в `services/api/app/models/`
- Возможно в `services/api/db/migrate/`
- Возможно в `services/api/app/services/postal_client.rb`

**Действия при конфликтах:**
1. Для моделей → **Брать из DB_Fix** (доп. ветка)
2. Для миграций → **Осторожно! Объединить вручную** (обе версии могут быть нужны)
3. Для PostalClient → **Брать из DB_Fix** (доп. ветка)

---

## ✅ ЧЕКЛИСТ ПЕРЕД КАЖДЫМ МЕРЖЕМ

- [ ] Я на ветке `main`
- [ ] `main` обновлена (сделал Pull)
- [ ] Нет незакоммиченных изменений
- [ ] Я знаю, что содержит мержимая ветка
- [ ] Я готов разрешать конфликты

---

## ✅ ЧЕКЛИСТ ПОСЛЕ КАЖДОГО МЕРЖА

- [ ] Все конфликты разрешены
- [ ] Все файлы помечены как разрешенные
- [ ] Merge commit создан
- [ ] Проверил логи: `git log --oneline -5`
- [ ] Проект компилируется (если применимо)
- [ ] Готов к следующему мержу

---

## 🚨 ЧТО ДЕЛАТЬ, ЕСЛИ ЗАПУТАЛИСЬ

1. **Отменить текущий мерж:**
   - В Git GUI найдите "Abort Merge" или "Cancel Merge"
   - Или в терминале: `git merge --abort`

2. **Вернуться к чистому main:**
   - Переключитесь на `main`
   - Сделайте `Reset` к последнему коммиту main
   - Начните заново

3. **Создать backup:**
   - Перед мержем создайте ветку: `git branch main-backup main`
   - Если что-то пошло не так, вернитесь: `git checkout main-backup`

---

## 📊 СВОДНАЯ ТАБЛИЦА ПОРЯДКА МЕРЖА

| № | Ветка | Приоритет | Ожидаемые конфликты | Главное правило |
|---|-------|-----------|---------------------|-----------------|
| 1 | `origin/claude/security-fixes-F6I8U` | ⭐⭐⭐ КРИТИЧНО | Контроллеры, SMTP, Docker | Брать из доп. ветки |
| 2 | `origin/claude/fix-tracking-unsubscribe-links-Fr6Lf` | ⭐⭐ Высокий | Tracking, API | Брать из доп. ветки |
| 3 | `origin/claude/fix-ai-analysis-display-6zxju` | ⭐⭐ Высокий | AI контроллеры, Views | Брать из доп. ветки |
| 4 | `dashboard_only` | ⚠️ Осторожно | Навигация, Dashboard | Проверить удаления! |
| 5 | `DB_Fix` | ⭐⭐⭐ КРИТИЧНО | Модели, Миграции, PostalClient | Брать из доп. ветки |

---

## 💡 СОВЕТЫ

1. **Мержите по одной ветке за раз** - не пытайтесь мержить все сразу
2. **Проверяйте после каждого мержа** - убедитесь, что все работает
3. **Делайте коммиты после каждого мержа** - не накапливайте изменения
4. **Используйте таблицу приоритетов** - она поможет при конфликтах
5. **Если сомневаетесь** - берите версию из дополнительной ветки (фиксы важнее)

---

**Удачи с мержем! 🚀**

