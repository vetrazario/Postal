# 📦 УСТАНОВКА НА СЕРВЕР - ПРОСТО И БЫСТРО

## 🎯 ДЛЯ СЕРВЕРА: Ubuntu 22.04, IP: 159.255.39.48, Домен: linenarrow.com

---

## ⚡ БЫСТРАЯ УСТАНОВКА (3 ШАГА)

### Шаг 1: Подключитесь к серверу

```bash
ssh root@159.255.39.48
```

### Шаг 2: Скачайте проект

**Вариант A - Через Git (если репозиторий настроен):**
```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> /opt/email-sender
cd /opt/email-sender
git checkout claude/setup-email-testing-YifKd
```

**Вариант B - Скопируйте файлы с локального компьютера:**
```bash
# На ВАШЕМ компьютере (не на сервере):
scp -r /home/user/Postal root@159.255.39.48:/opt/email-sender

# Потом на сервере:
cd /opt/email-sender
```

### Шаг 3: Запустите установку

```bash
sudo bash install.sh
```

**Скрипт спросит:**
```
Введите ваш домен: linenarrow.com
Введите email администратора: admin@linenarrow.com
Введите название организации: LineNarrow Company
Получить SSL сертификат Let's Encrypt? y
```

**⏱️ Ждите 10-15 минут...**

Скрипт автоматически:
- ✅ Установит Docker
- ✅ Настроит файрволл
- ✅ Создаст пароли
- ✅ Получит SSL сертификат
- ✅ Запустит систему
- ✅ Инициализирует базы данных

---

## 🎉 ПОСЛЕ УСТАНОВКИ

### ✅ В конце увидите:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Система успешно установлена и запущена!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 ИНФОРМАЦИЯ ДЛЯ ДОСТУПА:

🌐 Dashboard: https://linenarrow.com/dashboard
👤 Логин Dashboard: admin
🔑 Пароль Dashboard: <автоматически сгенерирован>

📧 SMTP RELAY ДЛЯ AMS:
Host: linenarrow.com
Port: 2587

📁 ВСЕ ПАРОЛИ СОХРАНЕНЫ:
/root/email-sender-credentials.txt
```

### ✅ Откройте Dashboard:

```
https://linenarrow.com/dashboard
```

### ✅ Создайте SMTP credentials для AMS:

1. В Dashboard → **SMTP Credentials**
2. Нажмите **"Generate New Credential"**
3. **СКОПИРУЙТЕ пароль!** (показывается только один раз)

### ✅ Настройте AMS Enterprise:

```
SMTP Host: linenarrow.com
SMTP Port: 2587
Security: TLS/STARTTLS
Username: <из Dashboard>
Password: <из Dashboard>
```

### ✅ Готово! Отправьте тестовое письмо

---

## 🔍 ПРОВЕРКА СИСТЕМЫ

### Проверить что всё работает:

```bash
# Статус контейнеров
docker compose ps

# Логи
docker compose logs -f

# Все пароли
cat /root/email-sender-credentials.txt
```

Должны работать все сервисы:
- ✅ postgres (healthy)
- ✅ redis (healthy)
- ✅ mariadb (healthy)
- ✅ rabbitmq (healthy)
- ✅ postal (healthy)
- ✅ api (healthy)
- ✅ nginx (up)
- ✅ smtp-relay (up)
- ✅ sidekiq (up)

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Контейнеры не запустились:

```bash
# Смотрим логи
docker compose logs

# Перезапускаем
docker compose down
docker compose up -d
```

### SSL сертификат не получен:

```bash
# Проверьте DNS (должен показать IP сервера)
dig linenarrow.com +short

# Попробуйте снова
sudo certbot certonly --standalone -d linenarrow.com -d www.linenarrow.com
```

### Не могу зайти в Dashboard:

```bash
# Проверьте что nginx работает
docker compose ps nginx

# Проверьте порты
sudo netstat -tlnp | grep -E '(80|443)'

# Проверьте файрволл
sudo ufw status
```

### Посмотреть все пароли:

```bash
cat /root/email-sender-credentials.txt
```

---

## 🔄 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Перезапустить систему
docker compose restart

# Остановить систему
docker compose down

# Запустить систему
docker compose up -d

# Посмотреть логи
docker compose logs -f api
docker compose logs -f smtp-relay
docker compose logs -f postal

# Использование ресурсов
docker stats
```

---

## 📞 ЧТО ДАЛЬШЕ?

### 1. Настройка AI Analytics (опционально)

```bash
# Получите API ключ: https://openrouter.ai
# Затем в Dashboard → Settings:
# - Включите AI Analytics
# - Вставьте API ключ
# - Выберите модель: Claude 3.5 Sonnet
```

### 2. Настройка Webhooks (для уведомлений в AMS)

```bash
# В Dashboard → Webhooks:
# - Добавьте URL вашего AMS
# - Выберите события: delivered, opened, clicked, bounced
# - Протестируйте
```

### 3. Мониторинг

```bash
# Проверяйте регулярно:
docker compose ps
docker compose logs --tail=50
df -h  # Свободное место
```

---

## ✨ ВСЁ ГОТОВО!

Теперь у вас есть:
- ✅ Полностью рабочая система отправки email
- ✅ Dashboard для управления
- ✅ SMTP Relay для подключения AMS (порт 2587)
- ✅ Postal для отправки писем
- ✅ Трекинг открытий и кликов
- ✅ AI аналитика (опционально)
- ✅ HTTPS с автообновлением сертификата
- ✅ Автозапуск при перезагрузке сервера

**Отправьте тестовое письмо из AMS и проверьте в Dashboard!** 🚀

---

## 📚 Дополнительная документация

- **QUICK_START.md** - Краткое руководство
- **FINAL_REPORT.md** - Полный технический отчет
- **TESTING_GUIDE.md** - Руководство по тестированию
- **/root/email-sender-credentials.txt** - Все пароли и ключи
